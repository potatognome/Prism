# tests/test_data/sample_params.yaml - Test fixture for prism_params module

parameters:
  null_threshold: 0.05
  date_output_format: "%Y-%m-%d"
  encoding: "utf-8"
  date_fields:
    - signup_date
    - last_login
  required_fields:
    - id
    - name

pipeline:
  stop_on_error: false
  max_rows: 1000
  verbose_changelog: true
